[workspace]
resolver = "2"
members = [
  "crates/orcal-core",
  "src-tauri",
]

# One licence for the whole workspace, inherited by every member crate so the
# published metadata can never disagree with the LICENSE file at the root.
[workspace.package]
license = "GPL-3.0-or-later"
